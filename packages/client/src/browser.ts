// Browser entry point. The base `Client` already defaults to the global `fetch`,
// which is always present in browsers, so no environment-specific wiring is needed.
export * from "./client";
export * from "./objects";
